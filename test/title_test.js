/**
 * Test case for title.
 * Runs with mocha.
 */
'use strict'

const title = require('../lib/title.js')
const assert = require('assert')
const co = require('co')

describe('title', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Title', () => co(function * () {

  }))
})

/* global describe, before, after, it */
