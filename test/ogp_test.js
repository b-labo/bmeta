/**
 * Test case for ogp.
 * Runs with mocha.
 */
'use strict'

const ogp = require('../lib/ogp.js')
const assert = require('assert')
const co = require('co')

describe('ogp', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Ogp', () => co(function * () {

  }))
})

/* global describe, before, after, it */
